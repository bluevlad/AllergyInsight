# ===========================================
# AllergyInsight Docker 환경 변수
# ===========================================
# 이 파일을 .env로 복사하고 실제 값을 입력하세요
# 중요: .env 파일은 절대 Git에 커밋하지 마세요!
#
# 사용법:
#   cp .env.example .env
#   # .env 파일을 편집하여 실제 값 입력
#   docker-compose up -d
# ===========================================

# -------------------------------------------
# 필수 설정 (Required)
# -------------------------------------------

# 데이터베이스 비밀번호 (필수)
# 강력한 비밀번호 사용 권장: openssl rand -base64 24
DB_PASSWORD=<YOUR_SECURE_DB_PASSWORD>

# JWT 시크릿 키 (필수)
# 강력한 키 생성: openssl rand -hex 32
JWT_SECRET_KEY=<YOUR_JWT_SECRET_KEY>

# -------------------------------------------
# 선택 설정 (Optional)
# -------------------------------------------

# Google OAuth (소셜 로그인 사용 시)
# https://console.cloud.google.com/apis/credentials 에서 발급
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# PubMed API (논문 검색 기능 강화)
# https://www.ncbi.nlm.nih.gov/account/settings/ 에서 발급
PUBMED_API_KEY=
PUBMED_EMAIL=

# Semantic Scholar API
# https://www.semanticscholar.org/product/api 에서 발급
SEMANTIC_SCHOLAR_API_KEY=

# OpenAI API (AI 요약 기능)
OPENAI_API_KEY=

# -------------------------------------------
# URL 설정 (기본값 사용 가능)
# -------------------------------------------
FRONTEND_URL=http://localhost:4040
BACKEND_URL=http://localhost:9040
